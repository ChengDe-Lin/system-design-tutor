# Confusion Ledger - Blind Spot & Misconception Tracker

> This ledger is automatically updated after each deep-dive session.
> Review entries periodically to reinforce corrections and close knowledge gaps.

| 日期 | 主題 | 我的盲區/錯誤認知 | 核心正解 (一句話點醒) | 複習建議 |
|------|------|-------------------|----------------------|----------|
| 2026-03-17 | (範例) Message Queue | 以為 Kafka 是「推送」模型，適合低延遲即時通知 | Kafka 是 Pull-based append-only log，Consumer 主動拉取；低延遲推送場景應考慮 RabbitMQ 或專用 Pub/Sub | 重讀 `components/message_queue.md` 的底層實作差異段落，畫出 Kafka Consumer Group 的 polling 流程 |
| 2026-03-20 | Kafka Partition | 對 Broker、Topic、Partition 三者的關係不夠清晰；以為需要手動指定 partition 放在哪個 broker | Broker 是物理機器，Topic 是邏輯分類，Partition 是平行處理單位。Kafka 自動將 partition 分散到 broker，你只需設定 partition 數量和 replication factor | 重讀 `components/message_queue.md` Kafka 段落，畫出 3 broker × 3 partition × RF=3 的分佈圖 |
| 2026-03-20 | Kafka Partition Scaling | 想用 consistent hashing 解決 partition 動態擴展的問題 | Consistent hashing 解決的是「搬移少量資料」，但 Kafka 的核心問題是 append-only log 不搬移舊資料，加 partition 後同一 key 的 event 分散在兩個 partition，順序斷裂無法拼回。正解是一開始就開足夠多的 partition（預期 consumer 數的 2-3 倍） | 思考：如果必須加 partition，什麼情境可以直接加（不依賴 key ordering）、什麼情境需要 topic migration |
| 2026-03-25 | Ticketmaster — Redis TTL | 沒想到用 Redis TTL 管理時效性狀態（如座位鎖定 15 分鐘過期自動釋放） | Redis TTL 是管理 temporary state 的基本手段：設定 key 時附帶 TTL，過期自動刪除，不需要額外的 cron job 或 timer。座位鎖定、購物車保留、OTP 驗證碼等「有時效性的狀態」都適用 | 遇到「某狀態需要在 N 分鐘後自動失效」的需求時，第一反應應該是 Redis TTL |
| 2026-03-25 | Ticketmaster — CDN 加速 | 沒想到對 event/票務資訊掛 CDN cache | 票務系統的 event 頁面（演出資訊、場地圖、票價階層）是 read-heavy 且更新頻率低的內容，非常適合 CDN cache + 短 TTL 或 SWR。開賣瞬間的流量 spike 大部分是讀取 event 資訊，CDN 可以擋掉 90%+ 的 origin 請求 | 設計 read-heavy 系統時，先問「哪些資料可以 CDN cache？」——靜態資訊（商品頁、活動頁）幾乎都可以 |
| 2026-03-25 | Ticketmaster — 即時狀態推送 | 沒想到需要用 WebSocket/SSE 即時更新 client 的票務狀態 | 使用者選位後等待付款、其他人搶同一區的座位——這些狀態變更需要即時推送到 client。不用 bidirectional connection 的話，client 只能 polling，體驗差且浪費資源。SSE 適合這種 server → client 單向推送場景 | 遇到「使用者需要看到即時狀態變更」的需求時，主動考慮 WebSocket（雙向）或 SSE（單向推送） |
| 2026-03-25 | Uber — Geospatial Index 選型 | 直覺用 PostgreSQL GiST index 做司機位置查詢，沒考慮到寫入量級（~1.5M writes/sec）遠超 PostgreSQL 單節點能力；也沒意識到 QuadTree 的 update = delete + re-insert 在高頻更新下有 lock contention 問題 | 高頻位置更新場景應用 Redis GEOADD（Sorted Set + GeoHash，O(log N) write，100K+ ops/sec/node）+ 按城市 shard。PostgreSQL/QuadTree 適合 read-heavy 且低頻更新的空間資料（餐廳、商店）。GeoHash 的 uneven cell 問題可用 adaptive precision 或 Google S2 (Hilbert curve) 緩解 | 設計 location-tracking 系統時，先估算 write QPS：如果 >10K writes/sec，排除傳統 DB geo index；考慮 Redis Geo 或 S2-based in-memory index |
| 2026-03-25 | Uber — Redis QPS 估算 ⚠️ **重複命中 2 次 (2026-04-15 又問單節點 QPS 量級)** | 引用「Redis 在這個場景 1M QPS」時，沒有區分這是單節點還是 cluster 整體的數字；第二次在 Uber 題複習時又忘記單節點具體數字 | Redis 單節點：simple GET/SET 100-200K ops/sec、GEOADD/ZADD 50-100K、pipeline 可到 500K-1M。1M QPS 需要 10-20 個節點按城市 shard。**引用 QPS 數字必須三問：(1) per-node 還是 aggregate？(2) workload 類型 (simple vs 複雜)？(3) 什麼硬體？** 三問缺一就不要信那個數字 | **已升級為高優先複習項目**。完整速查表見 `components/single_node_throughput.md`。每次設計前先查該表確認量級，不要憑感覺 |
| 2026-03-25 | Uber — Redis 不適合當 Primary DB | 知道不會用 Redis 當 primary DB，也正確判斷核心與 in-memory 特性有關，但未深入拆解具體原因（durability gap、成本差距、query 限制） | 三個原因依致命程度排序：(1) Durability — AOF everysec 仍可能丟 1 秒資料，RDB 丟更多；(2) 成本 — RAM 比 SSD 貴 10-30 倍，TB 級資料不可行；(3) Query — 無法 JOIN/aggregation/複雜查詢。但在 Uber location 場景 Redis 反而完美，因為這筆資料本身是 ephemeral disposable state，3 秒後就會被覆蓋 | 選 storage 時問：「這筆資料丟了會怎樣？」如果答案是「沒差，很快會有新的」→ Redis 很適合；如果「不可接受」→ 需要 disk-based DB |
| 2026-03-27 | Dropbox — 裝置同步通知機制 | 想到 client 可以主動打 API check diff（data channel），但沒想到需要一個 lightweight notification channel 來觸發 client 去拉資料；把問題簡化成「WebSocket vs Polling」二選一 | 正解是 **通知與資料分離**：Notification Channel（long polling，幾 bytes signal）告訴 client「有變更」，Data Channel（GET /delta）讓 client 拉取實際 diff。Long polling 是最佳平衡點 — 比 periodic polling 即時、比 WebSocket 簡單，且不需要維護 persistent connection | 設計 sync 系統時，先拆成兩條路：(1) 怎麼通知 client 有變更？(2) client 怎麼拿到變更內容？前者要輕量即時（long polling），後者要完整可靠（REST API + batching） |
| 2026-03-28 | Dropbox — Chunk Deduplication | 知道 file chunking，但漏掉 dedup 機制：client 上傳前先送 hash list 讓 server 比對，已存在的 chunk 不需要重傳；cross-user dedup 讓相同檔案全平台只存一份 | Dedup 是 Dropbox 的核心差異化：上傳流程是 check_hashes → server 回缺哪些 → 只傳缺的。配合 content-defined chunking（Rabin fingerprint），即使檔案中間插入內容，大部分 chunk hash 不變，dedup 不會失效。Fixed-size chunking 在插入場景下所有 chunk 全部偏移、hash 全變 | 設計 file storage 系統時，先問「怎麼避免存重複資料？」→ chunk-level dedup by hash 是標準做法。進階：content-defined chunking > fixed-size chunking |
| 2026-03-28 | Dropbox — Conflict Resolution | 知道有衝突問題，但沒想清楚偵測機制（CAS on version）和為什麼不做 auto-merge | 偵測：client 上傳帶 expected_version，server 做 compare-and-swap，version 不符 = 衝突。處理：建立 conflicted copy，不做 auto-merge — 因為 binary 檔無法 merge，text merge 也可能語意錯誤。Auto-merge 是 Google Docs 的領域（OT/CRDT），Dropbox 定位是 file sync 不是 real-time collaboration | 遇到 concurrent write 問題時，先選偵測機制（version CAS / vector clock），再選解決策略（last-write-wins / conflicted copy / auto-merge），並說明為什麼 |
| 2026-03-28 | Dropbox — Delta Sync 限制 | 知道 delta sync 概念但沒想到對壓縮檔和加密檔幾乎無效 | Delta sync 用 rolling checksum 比對新舊 chunk 的差異，只傳不同的 bytes。但壓縮檔（zip/gzip）改一點 → 壓縮後整個 byte stream 變化；加密檔改一點 → avalanche effect 導致密文全變。所以 .docx（本質是 zip）的 delta sync 效果有限 | 提到 delta sync 時主動說明適用範圍：text/code/CSV 效果好，壓縮檔和加密檔效果差 |
| 2026-03-28 | ⚠️ 通用盲區 — CDN 加速 | **第三次漏掉 CDN**（Ticketmaster event 頁面、Ticketmaster 座位概況、bit.ly redirect）。每次設計 read-heavy 系統都沒有第一時間想到 CDN | **任何 read-heavy 系統的第一個設計動作應該是問「哪些東西可以 CDN cache？」** 靜態頁面、不常變的 API response、redirect 結果都可以 cache。CDN 是最便宜的 scale 手段 — 一層 CDN 可以擋掉 90%+ 的 origin 請求 | 這是一條通用 checklist item，不是個別場景問題。畫完架構圖後，掃一遍每個 read path，問「這個 response 可以 CDN cache 嗎？TTL 多少合理？」 |
| 2026-03-28 | bit.ly — 301 vs 302 Redirect | 知道有 redirect status code 但記不住 301/302 的語意差異 | **301 = Moved Permanently**（瀏覽器 cache，下次不再問 server）；**302 = Found / Moved Temporarily**（不 cache，每次都問 server）。URL shortener 選哪個取決於是否要做 analytics：要追蹤點擊用 302，不需要用 301 | 301 = 永久 = 瀏覽器記住 = 你丟失 analytics；302 = 暫時 = 瀏覽器每次問 = 你保留 analytics。bit.ly 商業模式是賣 analytics，所以用 302 |
| 2026-03-28 | ⚠️ 通用盲區 — Read/Write Service 分離 | 每次都把 read 和 write 放在同一個 service 裡 scale，沒有第一時間根據 read:write ratio 拆開獨立 scaling | **當 read:write ratio > 10:1 時，應該拆成獨立 service 分開 scale。** 好處：(1) 各自用最合適的 pod 數量，不浪費資源；(2) dependency 不同可以獨立優化（read → cache/replica, write → primary DB）；(3) failure isolation — 一邊掛了不影響另一邊 | 估完 QPS 後，看 read:write ratio。> 10:1 就該考慮拆。這跟 CDN 一樣是 checklist item，不是深度設計問題 |
| 2026-03-30 | WhatsApp — 多對多關係用 array 欄位 | 想在 chats table 裡用 BIGINT[] 或 JSONB 存 member_ids，沒意識到「user 在哪些 chat」的反向查詢無法建有效 index → 全表掃描 | **多對多關係永遠用 mapping table**（chat_members: chat_id + user_id）。兩個方向都能建 index：(chat_id, user_id) 查成員、(user_id, joined_at) 查某人的聊天室。Array 欄位內的值無法被 B-tree index 有效查詢，即使 DB 支援 array 也不該在高頻查詢場景使用 | 遇到「A 包含多個 B」的關係時，永遠先問反向查詢「給定 B，找所有包含它的 A」能不能走 index。如果不能 → mapping table |
| 2026-04-14 | Instagram — Redis Set 生命週期與 Source of Truth | 看到 Like 計數器設計用 Redis counter + Set 做 dedup，以為 Set 會永遠留在 Redis；也擔心 Redis down 掉資料就全毀——沒區分 counter (aggregate, 可重建) 和 liked_by Set (primary data, 需持久化) 的不同語意 | **Redis 永遠只是加速層，真相層必須在 DB。** Counter 可從 DB `posts.like_count` 或 `likes` 明細表重算 (可容忍 ≤flush 週期的誤差)。Set 是 dedup 熱路徑，hot post 用 TTL / LRU 留在 Redis，cold post 驅逐後 dedup fallback 查 DB `likes` 表，百萬級大 post 改用 Bloom Filter 省記憶體。寫入 path 必須同步或透過 Kafka 非同步寫 `likes` 表，這才是 source of truth；Redis 資料掉了都能從 DB 重建 | 遇到任何「用 Redis 做計數 / dedup / index」的設計，永遠先問：(1) 這筆資料是 aggregate 還是 primary？(2) source of truth 在哪？(3) Redis 全掉了怎麼重建？如果任何一題答不出來，表示架構有洞 |
| 2026-04-14 | Instagram — Redis Counter 的真正目的 | 以為 Redis counter 的目的是「減少 DB 寫入總量」；搞混「明細 INSERT」和「彙總 UPDATE」兩個不同寫路徑的特性 | **Redis counter 不是為了減少寫入量，是為了消除 hot-row UPDATE 爭用。** 這個場景有兩條寫 path：(A) INSERT 到 `likes` 明細表——每筆新 row、無 lock 爭用、sharding + Kafka batch 可撐；(B) UPDATE `posts.like_count` 彙總欄位——viral post 全部打同一 row、row lock 排隊、DB 會崩。Redis INCR 是 O(1) 無爭用的原子操作，把 path B 從 58K UPDATEs/sec 壓到每 30s 一次 final value flush。明細 INSERT 仍然會打 DB（透過 Kafka consumer 批次），因為它沒有 hot-row 問題 | 設計 counter 類系統時依序問：(1) 寫是否集中在少數 row (hot row test)？YES → 必要加 Redis；(2) 讀 QPS 有多高？高 → Redis 加速讀；(3) 能容忍多少誤差？社交可 30s flush、金融不行。把「寫量大」和「寫集中」分開看 |
| 2026-04-14 | OLTP vs OLAP — `likes` 表存哪裡 | 看到 `likes` 表 append-only、欄位少、可 aggregation，直覺想用 OLAP (ClickHouse/Druid/BigQuery)；漏掉了 dominant query pattern 是 point lookup (dedup) 和 single-row write | **選 OLTP/OLAP 不看「資料長什麼樣」，看「dominant query pattern 是什麼」。** Likes 最頻繁的操作是「user X 有 like post Y 嗎？」(PK point lookup) 和 single-row INSERT/DELETE，OLAP 在這兩件事上都會死：columnar scan 讓 point lookup 達 100ms+、高 QPS single insert 拖垮 merge engine (ClickHouse `Too many parts`)、DELETE 要重 mutation。正解是 OLTP (sharded MySQL / Cassandra) 當 source of truth，再用 CDC (Debezium) → Kafka → OLAP 做下游 analytics (creator insights, trending velocity)——OLAP 有它的位置，但在分析管線不在主存 | 判斷表放 OLTP 還是 OLAP 的 checklist：latency 要求 (ms vs 秒)、寫入 pattern (single-row QPS vs batch)、是否有 point lookup、是否需要 DELETE/UPDATE 單筆、是否需要 transaction。7 題超過 4 題是 OLTP 答案 → 放 OLTP，剩下的用 CDC 流到 OLAP |
| 2026-04-27 | WebSocket Gateway 容量推導 | 看到「一個 Gateway 承載 ~100K WebSocket 連線」直覺想記下來當數字背，沒想到要從 first principles 推導 | **長連線容量不背，要從 4 個瓶頸推：(1) 記憶體 ~30-50 KB/conn（幾乎一定先撞牆）→ 100K × 40KB ≈ 4 GB；(2) FD：調 ulimit 到 1M+ 即可，非物理極限；(3) Port：server 不受 65535 限制（4-tuple 區分），破解迷思；(4) CPU：idle 不耗，活躍訊息才耗——100K conn × 1 msg/sec = 100K msg/sec 接近單核極限**。「100K/node」是 idle-heavy workload 的 anchor，高訊息率場景（即時遊戲、股價）可能只剩 10-20K。Erlang/Elixir 等可到 500K-2M（WhatsApp 做過 2M/server） | 完整速查見 `components/single_node_throughput.md` 第 8 節。設計時順序：估記憶體 → 估 message rate × 連線數 → 算 CPU → 算需要幾台。**永遠分開講「連線數」和「訊息率」**，混在一起量級會錯 |
| 2026-04-27 | CRDT 是否需要 transform 游標位置 | 看到 OT 要 transform 游標座標的描述，直覺猜「CRDT 也需要類似 transform」，沒意識到 CRDT 把位置語意整個換掉了 | **CRDT 不需要 cursor transform，是因為「位置」的定義從 integer index 換成 RelativePosition (anchor 在某個 char 的 globally unique ID 之後)**。OT cursor = 絕對座標 (5)，前面編輯一改就要修；CRDT cursor = 相對 anchor (在 5@A 之後)，anchor 不會被別人動到，render 時 lookup 它的 visual index 自動正確。Edge case：anchor 被刪 → 變 tombstone，fallback 到下個活字元；range selection 兩 anchor 獨立反而比 OT 乾淨。Yjs 的 `RelativePosition` API 和 Automerge 的 cursor API 就是這個機制 | 比較 OT/CRDT 時要拆「文件 op」和「cursor」兩條路徑各自怎麼處理，不要假設一邊有的另一邊也有。設計時問：「這個座標是絕對的還是相對的？絕對的就要 transform，相對的就不用」 |
| 2026-04-27 | Trade-off 總結表缺少「scope of validity」 | 寫設計總結表時直接列「衝突解決 = OT」，把它寫成普世答案，跳過了「這個選擇成立的前提條件」這層脈絡——前面 §4 已經給了 decision tree (P2P / 離線 / 文件大小 / 團隊經驗 → 不同答案)，但 §13 結論欄又把 OT 當唯一解 | **每個技術選擇都要標明 scope of validity——「在什麼條件下這是答案」**。寫 trade-off 表的正確姿勢：(1) 開頭明列「本設計的前提條件」(2) 每行的「選擇」要配「條件變了會怎樣」(3) 結尾附決策快查 (依需求條件選 A 或 B)。否則總結表退化成 cookbook，跟前面分析脫節。寫完後自我檢查一次：「如果這個系統的 X 條件變成 Y，這張表還對嗎？」對不上的就要加 column | 寫任何 trade-off / 設計決策表時，永遠問：「這個選擇是因為什麼條件成立？條件變了該怎麼選？」沒有 scope 的答案 = 危險的權威 |
| 2026-04-27 | CRDT 衝突解決：「自動 merge」是誤導 | 看到「CRDT 每個字元有 unique ID → merge 自動決定順序」就以為 CRDT 真的沒衝突；直覺 (正確) 質疑：兩個 client 同時在 A、B 之間插字，相對位置 (origin_left=A, origin_right=B) 完全一樣時怎麼辦 | **CRDT 不是「沒衝突」，是「衝突的解決邏輯內建在 ID 的 total order 規則裡」**。Yjs 用 YATA：當兩個 item 的 origin 相同 → 比較 clientID 數值，小的排前面。其他演算法的 tie-break：RGA 用 (timestamp, replica ID)、Logoot/LSEQ 用 fractional ID、Treedoc 用 binary tree path。**ID 結構 = (clientID, clock)，ID 本身永不重複（random clientID + 單調 clock），但相對位置可以一樣 → 必須有 tie-break**。這個 tie-break 規則 deterministic，任何 replica 獨立算出同樣結果——這才是 P2P 不需中央 server 的真正原因，不是「沒衝突」 | 講 CRDT「無衝突」時要拆兩層：(1) ID 層唯一性（clientID + clock 保證）(2) 位置層 tie-break（YATA / RGA / fractional ID 各有 deterministic 規則）。任何時候講 CRDT「自動」都要追問：「自動的依據是什麼？」答案永遠是某種 deterministic total order |
